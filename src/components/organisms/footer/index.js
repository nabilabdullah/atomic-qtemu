import React from 'react'

export default function Footer({text}) {
  return (
    <div class="row mt-5">
        <div class="col-12 py-5 text-center bg-light font-weight-bold">{text}</div>
    </div>
  )
}
